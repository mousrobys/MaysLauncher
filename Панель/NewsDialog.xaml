<Window x:Class="LauncherPanel.NewsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создать новость" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Заголовок:" Foreground="{StaticResource Muted}" Margin="16,16,16,4"/>
        <TextBox Grid.Row="1" x:Name="TxtTitle" Margin="16,0"/>

        <TextBlock Grid.Row="2" Text="Текст:" Foreground="{StaticResource Muted}" Margin="16,12,16,4" VerticalAlignment="Top"/>
        <TextBox Grid.Row="2" x:Name="TxtContent" Margin="16,32,16,0" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

        <CheckBox Grid.Row="3" x:Name="ChkImportant" Content="Важная новость" Foreground="{StaticResource Text}" Margin="16,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
            <Button Content="Отменa" Click="BtnCancel_Click" Padding="16,8" Background="{StaticResource Card}" Foreground="{StaticResource Text}"/>
            <Button Content="Сохранить" Click="BtnSave_Click" Padding="16,8" Margin="8,0,0,0"/>
            <Button Content="Опубликовать" Click="BtnPublish_Click" Padding="16,8" Margin="8,0,0,0" Background="{StaticResource Green}"/>
        </StackPanel>
    </Grid>
</Window>
